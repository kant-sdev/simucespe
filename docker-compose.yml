services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      PORT: "8000"
      BACKEND_CORS_ORIGINS: "http://localhost:5173,http://127.0.0.1:5173"
    volumes:
      - ./data/runtime:/app/data/runtime

